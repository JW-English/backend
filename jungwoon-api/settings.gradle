plugins {
    // 로컬에 JDK 21이 없으면 Gradle이 자동으로 내려받도록 한다.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'jungwoon-api'

include 'common'
include 'domain'
include 'infra'
include 'api'
